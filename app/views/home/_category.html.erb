<div id="<%= dom_id category %>">
  <p class="my-5">
    <%= link_to category.name, category_products_path(category_id: category.id), class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  </p>
</div>
